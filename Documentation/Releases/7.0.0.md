# API

## Channel Data

- Add support for concatenating `ChannelData` with `+` operator
- Add method `ChannelData.values` to retrieve all values stored in a channel data object

# Package

- Do not load tables package for basic CAN code like `can.connection` to improve general import loading time
- Remove `pytest` imports to improve general loading time
