# Logging

- Change namespace of CAN message logger from `icotronic.can` to `icotronic.can.messages`
- Use module name (`__name__`) for logger name to make it easier to filter logging messages
- Add additional logging statements to make debugging easier
- Add logging information to documentation
